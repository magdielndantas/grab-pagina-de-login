function logInn()  {
  }
  
const email = document.querySelector('email');
const password = document.querySelector('password');
email.addEventListener('Entrar', logInn);
password.addEventListener('Entrar', logInn)